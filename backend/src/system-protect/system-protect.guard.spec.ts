import { SystemProtectGuard } from './system-protect.guard';

describe('SystemProtectGuard', () => {
    it('should be defined', () => {
        expect(new SystemProtectGuard()).toBeDefined();
    });
});
